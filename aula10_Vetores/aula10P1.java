package aula10_Vetores;

public class aula10P1 {
public static void main(String[] args) {
/*
-Vetores
   Um vetor (array) é uma estrutura que permite armazenar vários valores do mesmo tipo dentro de uma única variável.
   Sua sintaxe é:

   *Criando um vetor vazio
   tipo[] nome = new tipo[n];

   n = quantidade de elementos no array

   *Dando valores ou alterando
   nome[0] = 7.5;
   nome[1] = 8.0;
   nome[2] = 6.5;

   *Criando um vetor com dados
   int[] nome = {1, 2, 3, 4, 5};


   
   Para acessar usamos a seguinte sintaxe:
   *System.out.println(numeros[0]);
   
   *numeros[0] -> representa o valor na posição desejada
   
-Propriedades importantes
   
   *Quantidade de elementos no Array 
      nome.length

   *To String
      import java.util.Arrays;

      Arrays.toString(nome)

   *Ordenação
      Arrays.sort(nome);

   *Preencher
      Arrays.fill(nome, valor);

   *Copiar
      Arrays.copyOf(nome, total elementos)

   *Comparar conteúdo
      Arrays.equals(a, b)

-For Each
   Há uma mneira de passar por elementos sem usar um "for" padrão com a seguinte sintaxe:

*   for (tipo elemento : lista) {
*      <comando>
*   }


*/ 
}
}
